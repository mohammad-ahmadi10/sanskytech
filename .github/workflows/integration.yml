name: sanskytech-CICD
'on':
  push:
      branches:
      - master
  pull_request:
      branches:
      - master

jobs:
    build:
        runs-on: self-hosted
        steps:
            - name: checkout 
              uses: actions/checkout@v3
            - name: build project
              run: |
                yarn
                yarn build
            
